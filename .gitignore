# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db
desktop.ini

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

tests/_output/*
tests/_support/_generated

# vagrant folder
/.vagrant

# runtime & cache (logs, sessions, cache)
/runtime/*
/logs/*
!/runtime/.gitignore

# generated web assets
/web/assets/*
!/web/assets/.gitignore

# environment config (may contain secrets)
.env
.env.local
.env.*.local

# Python virtual env (if any)
/.venv

# Node modules
node_modules/
scripts/sog5role/node_modules/

# SQLite runtime db
scripts/runtime/*.db